provider:
  name: example-provider
  description: This is an example provider
  type: Internet # default Internet
  host:
    dev: example.com
    tst: example.com
    acc: example.com
    prd: example.com
  port: 443 # default 443
  useSsl: true # default true
  path: /v1/test
  keyStore: openshift
  keyAlias: openshift
  managedByProxy: true